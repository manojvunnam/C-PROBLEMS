#include <stdio.h>
int main() {
int a;
char b;
float c;
double d;
long e;
unsigned f;
long double g;
signed int h;
printf("integer is%d\n",sizeof(a));
printf("char is%d\n",sizeof(b));
printf("float  is%d\n",sizeof(c));
printf("double is%d\n",sizeof(d));
printf("long is%d\n",sizeof(e));
printf("unsigned int is%d\n",sizeof(f));
printf("long double is%d\n",sizeof(g));
printf("signed int is%d\n",sizeof(h));
    return 0;
}
