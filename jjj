#include <stdio.h>
int countdigits(int num){
    int count=0;
    if(num==0)
    return 1;
while(num!=0){
num=num/10;
    count++;
}
return count;
}
int main(){
    int num;
scanf("%d",&num);
printf("%d",num);
printf("count=%d",countdigits(num));
    return 0;
}
