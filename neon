#include <stdio.h>
#include<math.h>
int main() {
    int n;
    scanf("%d",&n);
    int square=n*n;
    int temp=square;
    int sum=0;
    while(temp!=0){
        sum=sum+temp%10;
        temp=temp/10;
    }
    if(sum==n){
        printf("neon number");
    }else{
        printf("not neon number");
    }
    return 0;
}
