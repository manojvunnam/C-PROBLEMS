#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* addBinary(char* a, char* b) {
    int i = strlen(a) - 1;
    int j = strlen(b) - 1;

    int maxLen = (strlen(a) > strlen(b)) ? strlen(a) : strlen(b);

    // Allocate space for result (+2 for possible carry and '\0')
    char *result = (char *)malloc(maxLen + 2);

    int k = maxLen + 1;
    result[k] = '\0';

    int carry = 0;

    while (i >= 0 || j >= 0 || carry) {
        int sum = carry;

        if (i >= 0)
            sum += a[i--] - '0';
        if (j >= 0)
            sum += b[j--] - '0';

        result[--k] = (sum % 2) + '0';
        carry = sum / 2;
    }

    // Shift result to remove unused space
    char *finalResult = (char *)malloc(strlen(result + k) + 1);
    strcpy(finalResult, result + k);

    free(result);

    return finalResult;
}
