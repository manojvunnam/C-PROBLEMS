#include<stdio.h>
int main(){
char str[100];
char *start,*end;
int length=0,flag=1;
scanf("%s",str);
start=str;
while(*(str+length)!='\0'){
length++;
}
end=str+length-1;
while(start<end){
if(*start!=*end){
flag=0;
break;
}
start++;
end--;
}
if(flag)
printf("yes");
else
printf("no");
return 0;
}
